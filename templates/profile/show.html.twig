{% extends 'base.html.twig' %}

{% block title %}User Profile{% endblock %}

{% block body %}
    <div class="container">
        <div class="row mt-5">
            <div class="col-4">
                <p>
                    <img src="{{ app.user.avatar }}" alt="" width="100%">
                </p>
                {% for role in app.user.roles %}
                    {% if role != 'ROLE_USER' %}
                        <p>
                            <span class="fa fa-star"></span>
                            <i>{{ role }}</i>
                        </p>
                    {% endif %}
                {% endfor %}
            </div>
            <div class="col-8">
                <h3>{{ app.user.fullName }}</h3>
                <p>Enabled: {{ app.user.enabled ? 'yes' : 'no' }}</p>
                <p>Email: {{ app.user.email }}</p>
            </div>
        </div>
    </div>
{% endblock %}
