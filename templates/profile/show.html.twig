{% extends 'base.html.twig' %}

{% block title %}User Profile{% endblock %}

{% block body %}
    <div class="container">
        <div class="row mt-5">
            <div class="col-3">
                <p>
                    <img src="{{ asset(app.user.avatarUrl) }}" alt="" width="100%">
                </p>
                {% for role in app.user.roles %}
                    {% if role != 'ROLE_USER' %}
                        <p>
                            <span class="fa fa-star"></span>
                            <i>{{ role }}</i>
                        </p>
                    {% endif %}
                {% endfor %}
            </div>
            <div class="col-9">
                <h3>{{ app.user.fullName }}</h3>
                <p>Enabled: <span class="badge badge-secondary">{{ app.user.enabled ? 'yes' : 'no' }}</span></p>
                <p>Email: {{ app.user.email }}</p>
            </div>
        </div>
    </div>
{% endblock %}
